<UserControl x:Class="WDOS.Modules.HardwareView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <!-- Title -->
            <RowDefinition Height="Auto" />
            <!-- System Cards -->
            <RowDefinition Height="*" />
            <!-- Status Console -->
            <RowDefinition Height="140" />
        </Grid.RowDefinitions>

        <!-- Title -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Donanım Tanılama &amp; Sistem Bilgisi" FontSize="22" FontWeight="Bold" Foreground="{StaticResource AccentBlue}" />
            <TextBlock Text="Windows Management Instrumentation (WMI) aracılığıyla donanım bilgilerini sorgular ve sistem sağlığını analiz eder." FontSize="13" Foreground="{StaticResource TextSecondary}" Margin="0,5,0,0" />
        </StackPanel>

        <!-- Main Cards Split -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Specifications Card -->
            <Border Grid.Column="0" Background="{StaticResource BgSidebar}" BorderBrush="{StaticResource BorderSlate}" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="Donanım Spesifikasyonları" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,15" />
                    
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="İşletim Sistemi:" FontWeight="Bold" Foreground="{StaticResource TextSecondary}" />
                        <TextBlock x:Name="TxtOs" Grid.Column="1" Text="Sorgulanıyor..." Foreground="{StaticResource TextPrimary}" TextWrapping="Wrap" />
                    </Grid>

                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="İşlemci (CPU):" FontWeight="Bold" Foreground="{StaticResource TextSecondary}" />
                        <TextBlock x:Name="TxtCpu" Grid.Column="1" Text="Sorgulanıyor..." Foreground="{StaticResource TextPrimary}" TextWrapping="Wrap" />
                    </Grid>

                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Bellek (RAM):" FontWeight="Bold" Foreground="{StaticResource TextSecondary}" />
                        <TextBlock x:Name="TxtRam" Grid.Column="1" Text="Sorgulanıyor..." Foreground="{StaticResource TextPrimary}" />
                    </Grid>

                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Ekran Kartı (GPU):" FontWeight="Bold" Foreground="{StaticResource TextSecondary}" />
                        <TextBlock x:Name="TxtGpu" Grid.Column="1" Text="Sorgulanıyor..." Foreground="{StaticResource TextPrimary}" TextWrapping="Wrap" />
                    </Grid>

                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Anakart Model:" FontWeight="Bold" Foreground="{StaticResource TextSecondary}" />
                        <TextBlock x:Name="TxtMotherboard" Grid.Column="1" Text="Sorgulanıyor..." Foreground="{StaticResource TextPrimary}" TextWrapping="Wrap" />
                    </Grid>

                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Disk / Sağlık:" FontWeight="Bold" Foreground="{StaticResource TextSecondary}" />
                        <TextBlock x:Name="TxtDisk" Grid.Column="1" Text="Sorgulanıyor..." Foreground="{StaticResource TextPrimary}" TextWrapping="Wrap" />
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Actions Card -->
            <Border Grid.Column="1" Background="{StaticResource BgSidebar}" BorderBrush="{StaticResource BorderSlate}" BorderThickness="1" CornerRadius="8" Padding="15" Margin="10,0,0,0">
                <StackPanel VerticalAlignment="Center">
                    <Button Content="Taramayı Yenile" Style="{StaticResource ModernButtonStyle}" Background="#334155" Margin="0,0,0,15" Height="40" Click="RefreshScan_Click" />
                    <Button Content="📄 HTML Donanım Raporu Oluştur" Style="{StaticResource ModernButtonStyle}" Background="{StaticResource AccentBlue}" Height="45" Click="CreateReport_Click" />
                    <TextBlock Text="Rapor masaüstünüze HTML dosyası olarak kaydedilir." FontSize="11" Foreground="{StaticResource TextSecondary}" Margin="0,10,0,0" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap" />
                </StackPanel>
            </Border>
        </Grid>

        <!-- Logs console -->
        <Border Grid.Row="2" Background="#020617" BorderBrush="{StaticResource BorderSlate}" BorderThickness="1" CornerRadius="8" Padding="10">
            <ScrollViewer x:Name="ScrollerLogs" VerticalScrollBarVisibility="Auto">
                <TextBox x:Name="TxtLogs" 
                         Background="Transparent" 
                         Foreground="{StaticResource AccentGreen}" 
                         FontFamily="{StaticResource Monospace}" 
                         FontSize="12" 
                         BorderThickness="0" 
                         IsReadOnly="True" 
                         TextWrapping="Wrap" 
                         AcceptsReturn="True" 
                         Text="[WDOS Donanım Tanılama] Hazır.&#x0a;" />
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
